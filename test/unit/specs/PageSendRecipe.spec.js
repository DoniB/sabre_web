// import Vue from 'vue'
// import SendRecipe from '@/components/PageSendRecipe.vue'

describe('PageSendRecipe.vue', () => {
  it('is pending')

  it('should render correct contents', () => {
    // const Constructor = Vue.extend(SendRecipe)
    // const vm = new Constructor().$mount()
    // expect(vm.$el.querySelector('div.md-title').textContent)
    //   .to.equal('Cadastro de Receitas')
    // expect(vm.$el.querySelectorAll('input').length)
    //   .to.equal(1)
    // expect(vm.$el.querySelectorAll('textarea').length)
    //   .to.equal(2)
    // expect(vm.$el.querySelectorAll('button').length)
    //   .to.equal(1)
    // expect(vm.$el.querySelectorAll('button div.md-button-content')[0].textContent)
    //   .to.equal(' Enviar ')
    // const input = vm.$el.querySelectorAll('input')
    // input[0].value = 'Some Recipe Name'
    // input[0].dispatchEvent(new Event('change'))
    // expect(vm.$data.form).to.equal('Some Recipe Name')
  })
})
