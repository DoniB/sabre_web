// import Vue from 'vue'
// import Index from '@/components/dashboard/Index.vue'

describe('dashboard/Index.vue', () => {
  it('is pending')
  it('should render correct contents', () => {
    // const Constructor = Vue.extend(Index)
    // const vm = new Constructor().$mount()
    // expect(vm.$el.querySelector('#dashboard-index').textContent.trim())
    //   .to.equal('dashboard index placeholder')
  })
})
